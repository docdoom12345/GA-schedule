name: Scheduling a Workflow

on:
  schedule:
    - cron: '24 9 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:

      - name: Run a script
        run: echo "This is a scheduled action!"
